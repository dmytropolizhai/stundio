<?xml version="1.0" encoding="utf-8"?>
<!-- One row of the all-day widget's list. Kept flat — RemoteViews rows are inflated per-item
     in the launcher's process, so a deep tree here is both slow and easy to lose silently. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/day_entry_root"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:paddingTop="6dp"
    android:paddingBottom="6dp">

    <ImageView
        android:id="@+id/day_entry_rail"
        android:layout_width="3dp"
        android:layout_height="28dp"
        android:layout_marginEnd="8dp"
        android:contentDescription="@null"
        android:scaleType="fitXY"
        android:src="@drawable/widget_rail" />

    <TextView
        android:id="@+id/day_entry_time"
        android:layout_width="64dp"
        android:layout_height="wrap_content"
        android:textColor="@color/widget_text_muted"
        android:textSize="11sp" />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical">

        <TextView
            android:id="@+id/day_entry_title"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:maxLines="1"
            android:textColor="@color/widget_text_strong"
            android:textSize="13sp"
            android:textStyle="bold" />

        <TextView
            android:id="@+id/day_entry_subtitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:ellipsize="end"
            android:maxLines="1"
            android:textColor="@color/widget_text_muted"
            android:textSize="11sp" />
    </LinearLayout>
</LinearLayout>
