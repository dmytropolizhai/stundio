<!--
  PLACEHOLDER MARK — replace this file with the real Mark.svg.

  The splash masks this file's alpha channel (`mask-image: url(/Mark.svg)`), so any
  self-contained SVG works: colours inside are irrelevant, only the drawn silhouette is.
  Keep the viewBox square-ish and the artwork edge-to-edge — the mask is `contain`-fitted
  and centered, so padding inside the file becomes dead space on screen.
-->
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 120 120" fill="none">
  <path
    d="M84 30C84 18 36 18 36 40C36 62 84 58 84 80C84 102 36 102 36 90"
    stroke="#000"
    stroke-width="16"
    stroke-linecap="round"
  />
</svg>
